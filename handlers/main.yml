---

- name: Restart xrdp
  ansible.builtin.systemd:
    name: xrdp
    state: restarted

- name: Restart xrdp-sesman
  ansible.builtin.systemd:
    name: xrdp-sesman
    state: restarted

- name: Reload sysctl
  ansible.builtin.command: sysctl --system
  changed_when: false
